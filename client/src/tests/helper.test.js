const helper = require('../lib/helper');

test('adds 1 + 2 to equal 3', () => {
  expect(helper.sum(1, 2)).toBe(3);
});

// test('convertObjectIntoSortedArray to return an array',()=>{
//   expect()
// })